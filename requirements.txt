# Core (Python 3.10+, PyTorch 2.x)
torch>=2.0
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.7

# Dev / test
pytest>=7.4
ruff>=0.4

# Optional: approximate nearest neighbour retrieval (falls back to torch.topk)
# faiss-cpu>=1.8

# Optional: serving demo
# fastapi>=0.110
# uvicorn>=0.29
